//Crea un módulo que contenga una función que reciba una cadena y devuelva el número de caracteres que contiene. Luego, crea otro archivo en el que llames a esta función con una cadena y muestres el resultado en la consola.

function caracteres(cadena) {
 
  return cadena.length;
}

module.exports = caracteres;
