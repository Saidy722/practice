
//Crea una función que tome un número como argumento y devuelva su doble. Luego, crea otro archivo en el que llames a esta función y muestres el resultado en la consola.

function numeroDoble(numero) {
  return numero * 2;
}

module.exports = numeroDoble; //EXportar archivo
